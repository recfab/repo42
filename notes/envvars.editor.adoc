= EDITOR variable

#envvar

* +++<input type="checkbox" class="task-list-item-checkbox" disabled="disabled">++++++</input>+++EDITOR: How do you handle flags?
 ** git handles `code --wait` fine, but `$EDITOR` doesn't

== References

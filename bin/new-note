#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

bin_dir=$(dirname "$0")

id=$(date "+%Y%m%d%H%M%S")

file="$bin_dir/../notes/$id.md"

cat << EOF > "$file"
# $@

* [ ] fill out this note

## References
EOF

$EDITOR "$file"
